import React from 'react';

/**
 * 自动生成的占位符页面
 * -------------------------
 * LLM 生成失败，错误信息: this.mcp.getPrompt is not a function
 * 请手动实现该页面。
 */
export default function HomePage() {
  return (
    <main style={{ padding: '2rem', border: '1px dashed red' }}>
      <h1>Home Page</h1>
      <p>The main landing page.</p>
      <p><em>该页面是自动生成的占位符，因为动态代码生成失败。</em></p>
      <pre style={{ color: 'red' }}>Error: this.mcp.getPrompt is not a function</pre>
    </main>
  );
}